# クエスト仕様書：6017 — 第17話「鉄の門、黄金の鍵」

## 0. ファイル概要

| 項目 | 値 |
|-----|-----|
| **Quest ID** | 6017 |
| **Slug** | `main_ep17` |
| **クエスト種別** | メインエピソード（Main） |
| **推奨レベル** | 45 |
| **難度** | 5 |
| **依頼主** | 帝国軍 |
| **出現条件** | 前提クエストクリア: main_ep16 / 必須滞在拠点: loc_holy_empire |
| **サムネイル画像** | `/images/quests/bg_spot_roland_core.png` |

※BGM、SE、進行中の背景画像などはノードごとに指定します。

---

## 1. クエスト概要

### 短文説明
```
公式メインシナリオ
```

### 長文説明
```
[要定義: フレーバーテキスト]
```

---

## 2. 報酬定義

**CSV記載形式:**
```
Gold:4500|Rep:10
```

---

## 3. シナリオノード構成

### 簡易フロー
```text
start
  └─ start
  └─ text1
  └─ text1_1
  └─ text2
  └─ text2_1
      ├─[「膨大な資産で通行証を提示する」]→ N/A
      ├─[「強行突破する」]→ N/A
  └─ text3
  └─ text3_1
  └─ battle
      ├─[神の操り人形たちを退ける]→ N/A
  └─ text_win
  └─ end_node
```

### ノード詳細

#### `start`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
次元の狭間を越え、神々の中枢たる「天界の玉座」へ至るには、4大国家が古代より秘匿管理してきた黄金の鍵が必要だ。
```
**次ノード:** `{type:text, bg:bg_spot_roland_core}` (auto-advance)
**params:**
```json
text1
```

---

#### `text1`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
君は始まりの地、聖帝国ローランの王城に辿り着いた。かつて小隊長ガウェインが命を賭して守ろうとしたこの国の中心。
```
**次ノード:** `{type:text, bg:bg_spot_roland_core}` (auto-advance)
**params:**
```json
text1_1
```

---

#### `text1_1`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
だが、玉座の間へ続く「鉄の門」は固く閉ざされ、重装備の近衛兵団が槍を交差させて君の行く手を阻んでいる。
```
**次ノード:** `{type:text, bg:bg_spot_roland_core}` (auto-advance)
**params:**
```json
text2
```

---

#### `text2`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
「ここから先は聖域である。帝の許可なき者の立ち入りは、いかなる理由があろうと死罪に処す」
```
**次ノード:** `{type:text, bg:bg_spot_roland_core}` (auto-advance)
**params:**
```json
text2_1
```

---

#### `text2_1`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
近衛隊長の眼には、狂信的な光が宿っていた。すでに各国の指導者層は、神々の精神支配を受けつつあるという噂は本当だったのだ。
```
**次ノード:** `{type:text, bg:bg_spot_roland_core}` (auto-advance)
**params:**
```json
choice1
```

---

#### `text3`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
君はこれまでの世代が血の滲むような思いで積み上げた資産（20
```
**次ノード:** `000G相当）と、長年の外交的影響力を証明する特約状を突きつけた。` (auto-advance)
**params:**
```json
{type:text, bg:bg_default}
```

---

#### `text3_1`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
どれほど狂信に囚われていようとも、国家を動かすシステムとしての「富と権力」の重みは無視できない。門兵は青ざめ、黙って震える手で道を開けた。
```
**次ノード:** `{type:text, bg:bg_spot_roland_core}` (auto-advance)
**params:**
```json
end_node
```

---

#### `battle`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
問答無用で剣を抜く。「ならば力ずくでこじ開けるまでだ」。近衛隊長が激昂し、殺到する近衛兵団との激戦の火蓋が切られた！
```
**次ノード:** `{type:battle, enemy_group_id:3}` (auto-advance)
**params:**
```json
choice2
```

---

#### `text_win`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
血に染まる王城の階段。君は立ちはだかる精鋭たちを薙ぎ払い、強引に分厚い鉄の門をこじ開けた。「黄金の鍵」はすぐそこだ。
```
**次ノード:** `{type:text, bg:bg_spot_roland_core}` (auto-advance)
**params:**
```json
end_node
```

---

#### `end_node`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_spot_roland_core`

**テキスト:**
```
ついに神域へと至る鍵を手に入れた。しかしその直後、城全体を揺るがすほどの地響きが起こり、世界中で神々による「大粛清の儀式」が本格稼働を始めたという絶望的な報せが入る。
```
**次ノード:** `{type:end_success}` (auto-advance)
**params:**
```json

```

---


