# クエスト仕様書：6014 — 第14話「落城の陽光」

## 0. ファイル概要

| 項目 | 値 |
|-----|-----|
| **Quest ID** | 6014 |
| **Slug** | `main_ep14` |
| **クエスト種別** | メインエピソード（Main） |
| **推奨レベル** | 30 |
| **難度** | 3 |
| **依頼主** | 冒険者ギルド |
| **出現条件** | 前提クエストクリア: main_ep13 / 必須滞在拠点: loc_haryu |
| **サムネイル画像** | `/images/quests/bg_slum.png` |

※BGM、SE、進行中の背景画像などはノードごとに指定します。

---

## 1. クエスト概要

### 短文説明
```
公式メインシナリオ
```

### 長文説明
```
[要定義: フレーバーテキスト]
```

---

## 2. 報酬定義

**CSV記載形式:**
```
Gold:3000|Rep:10
```

---

## 3. シナリオノード構成

### 簡易フロー
```text
start
  └─ start
  └─ text1
  └─ text1_1
  └─ text2
  └─ text2_1
  └─ text3
  └─ battle
      ├─[一歩も退かずに迎え撃つ]→ N/A
  └─ text_post_battle
  └─ end_node
```

### ノード詳細

#### `start`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_slum`

**テキスト:**
```
不死の傭兵王ヴォルグから「神殺しの覚悟」を受け取った直後だった。華龍神朝の首都周辺に、天界から何十本もの聖なる光の柱が狂ったように降り注ぎ始めた。
```
**次ノード:** `{type:text, bg:bg_slum}` (auto-advance)
**params:**
```json
text1
```

---

#### `text1`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_slum`

**テキスト:**
```
空が文字通り割れ、その裂け目から神々が遣わした「破滅の巨竜（巨兵）」が次々と降下してくる。それはもはや戦争ではなく、一方的な蹂躙――神の怒りの具現化だった。
```
**次ノード:** `{type:text, bg:bg_slum}` (auto-advance)
**params:**
```json
text1_1
```

---

#### `text1_1`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_slum`

**テキスト:**
```
「逃げろ！とにかく西へ走れェ！」繁栄を誇った東洋の大都市が、一瞬にして瓦礫と神の炎に包まれていく。大通りは阿鼻叫喚の地獄絵図と化していた。
```
**次ノード:** `{type:text, bg:bg_slum}` (auto-advance)
**params:**
```json
text2
```

---

#### `text2`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_slum`

**テキスト:**
```
「民間人を逃がせ！我々が殿（しんがり）を務める！」残存する傭兵や華龍の兵士たちが、決死の覚悟で巨竜たちの前に立ち塞がる。
```
**次ノード:** `{type:text, bg:bg_slum}` (auto-advance)
**params:**
```json
text2_1
```

---

#### `text2_1`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_slum`

**テキスト:**
```
彼らもまた、死の恐怖に震えている。だが、背後で泣き叫ぶ子供たちの声を背に受けてなお、一歩も退こうとはしなかった。
```
**次ノード:** `{type:text, bg:bg_slum}` (auto-advance)
**params:**
```json
text3
```

---

#### `text3`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_slum`

**テキスト:**
```
君もまた、その中の一人として最前線に立った。「……俺も、少しはガウェインのような顔立ちになってきたか」君は自嘲気味に笑い、剣を強く握り直す。
```
**次ノード:** `{type:text, bg:bg_slum}` (auto-advance)
**params:**
```json
battle
```

---

#### `battle`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_slum`

**テキスト:**
```
逃げ遅れた民の背中を守るため、君は迫り来る数機もの神の巨竜の前に立ちはだかった。これは勝つための戦いではない。時間を稼ぐための、「死の舞」だ。
```
**次ノード:** `{type:battle, enemy_group_id:5}` (auto-advance)
**params:**
```json
choice1
```

---

#### `text_post_battle`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_slum`

**テキスト:**
```
全身が血に塗れ、骨が軋む。何体の巨竜を地に伏せさせたかも覚えていない。だが、君の後ろにはもう、逃げ惑う民の姿はなかった。避難は完了したのだ。
```
**次ノード:** `{type:text, bg:bg_slum}` (auto-advance)
**params:**
```json
end_node
```

---

#### `end_node`
**演出パラメータ:**
- **BGM**: `[要定義]`
- **SE**: `[要定義]`
- **背景画像**: `bg_slum`

**テキスト:**
```
華龍の絢爛たる首都は完全に失われたが、多くの命を救い出すことには成功した。残るは、この理不尽な破壊を強いる元凶――天界への門を開き、元凶を断ち切ることだけだ。
```
**次ノード:** `{type:end_success}` (auto-advance)
**params:**
```json

```

---


