# Wirth-Dawn 開発・運用ルール (KI)

## 1. メンテナンスモードおよび Edge Middleware に関する技術的制約
- **Edge Middleware における JWT デコードの注意点**:
  - Edge Runtime (Next.js Middleware 等) では、Base64Url のデコードを行う `atob` がパディング `=` を欠いている場合に `DOMException` をスローするため、必ず事前に `while (base64.length % 4) { base64 += '='; }` などのパディング補正処理を挟むこと。
  - Supabase SSR が書き込むクッキー（`sb-*-auth-token`）はサイズが大きい場合に自動で分割保存（`*.0`, `*.1` 等）され、かつ値全体が Base64 エンコードされている場合がある。そのため、`auth-token` を名前に含むすべてのクッキーを収集・ソート結合した上で、JSON パースの前に一度 Base64 デコード処理を行うロジックを維持すること。
- **サーバーコンポーネント内でのイベントハンドラ厳禁**:
  - Next.js のサーバーコンポーネント（特に `/maintenance` などの独立画面）では、HTML 要素に `onMouseOver` や `onMouseOut` などのイベントハンドラを直接渡してはならない。これを行うと、サーバーレンダリング時にクラッシュ（500エラー）を引き起こす。
  - 代替として、`<style>` タグや Pure CSS (`:hover` クラス) を使用してスタイリングのインタラクションを実現すること。
- **タイムゾーンの明示的指定**:
  - メンテナンス画面などの日付表示では、サーバーが UTC タイムゾーンで動作している場合があるため、`new Date()` の `getHours()` を直接呼ぶとずれた時刻が表示される。必ず日本時間 (JST: UTC+9) のオフセットを明示的に加算し、`getUTC*` 系のメソッドを用いてフォーマットすること。

## 2. リリース検証およびメンテナンス手順
- **事前アカウント検証 of 徹底**:
  - メンテナンス（および緊急リリース）のデプロイ時には、必ず事前に登録されているホワイトリストアカウント（きたむ様アカウント。新ID: `5ad434ec-763f-473e-939f-14a5e9e1cc93`、旧ID: `c1cf67dd-527a-497e-bf88-ce10c2cb516f`）での動作確認を徹底すること。
  - **次回以降のメンテナンス検証時、必ず「きたむ様アカウント」で実際にログインし、正常にメンテナンスをバイパスしてゲームに入れることを本番環境で徹底検証する。**
  - **Googleなどの OAuth 認証が行われる前段階（認証クッキーが存在しない状態）**では自動バイパスが機能しない。そのため、以下の2ステップで検証を行うこと：
    1. **未認証状態**: 管理者用バイパス URL（`?bypass=<bypass-key>`）を経由させてブラウザにバイパス用クッキーを植え付け、メンテナンス画面を通過してログイン画面が表示されるか確認する。
    2. **認証完了状態**: 実際に「きたむ様アカウント」でGoogleログインを行い、メンテナンス制限に引っかからずに拠点（宿屋等）に入れるか、一般ユーザーが遮断されている状態で正常にゲームをプレイできるか確認する。
  - **コロシアムランキングにおける除外仕様**: きたむ様アカウント（新旧ID）は、コロシアムランキングのAPIレスポンスから自動的にフィルタリング（除外）されるよう実装されている。そのため、ランキング画面での表示テストや集計動作検証を行う場合は、別途一般テストアカウントを使用する必要がある。
- **環境間の逆マージ (バックポート) の確認**:
  - 本番環境（`main`）に直接適用されたマイグレーションやソースコードがある場合は、放置せず速やかに開発環境（`develop`）および PREVIEW DB へ逆マージし、環境間の不整合を防ぐこと。

## 3. 非同期処理と状態クリアに伴う競合（連打バグ）の防止
- **フロントエンドにおける多重遷移ガードの徹底**:
  - バトルの終了（勝利・敗北・撤退）時やクエスト結果の確定時など、サーバー通信を伴う非同期処理が行われるボタンは、必ずフロントエンド側で多重実行防止フラグ（`isTransitioning` / `isProcessingEndRef` 等）を導入し、ボタンの `disabled` 化と二重発火防止ガードを行うこと。
  - 非同期処理の中でメモリ上の遷移先情報（`successNodeIdRef.current` 等）をクリアする場合、クリアタイミングと再実行の競合により、クリア後の `null` が読み取られて安全性の低いフォールバック先（無条件でのクエストクリアノード等）へワープする恐れがある。
- **安全なフォールバック設計**:
  - 状態が不正クリアされた場合のフォールバック先は、無条件の成功・クリアノード（`end_success` 等）に設定してはならない。必ず直前の正常状態（バトル開始前のノード、あるいは戦闘発生マスなど）に戻すようフォールバック設計を徹底すること。
- **完了APIにおけるバトル検証トークンの必須化**:
  - コロシアムなどの連戦・特殊クエストであっても、完了API（`/api/quest/complete`）でバトル検証トークンによる署名チェックがスキップされないよう、モック定義時にも適切な `script_data.nodes` を構築し、サーバーサイドでの改ざん・ワープ防止策を確実に作動させること。

- **クエスト状態のローカルキャッシュ初期化**:
  - クエストページ（`page.tsx`）初期化時、サーバー側の `current_quest_state` が `null` である場合は、ブラウザのZustand永続化キャッシュ（`quest-storage`）の状態に関わらず、必ず `startQuest` を呼び出してローカルの進行データを強制リセットし、ゾンビデータによる進行度のズレや同期上書きを防ぐこと。

- **Zustand セレクタによるパフォーマンス最適化**:
  - 巨大なコンポーネント（特に `BattleView.tsx` などの大規模UI）では、Zustandの `useGameStore()` をセレクタなしで呼び出してはならない。ストア全体の不要な更新を監視してしまい、超高頻度の再レンダリングが発生して動作がカクつく原因となるため、必ず個別にセレクタ形式（`state => state.property`）で必要なプロパティだけを個別に切り出して参照すること。

- **バトル撃破とアニメーション演出の同期**:
  - 敵の死亡表示（スプライトのモノクロ・ぼかし化）は、攻撃エフェクトのアニメーション（`activeEffect`）が再生されている途中で即時反映されてはならない。エフェクトの再生終了（`activeEffect` のクリア）を待って生存状態を維持させ、演出のぶつ切りを防止すること。

- **バトル終了時のログ再生完了ガード**:
  - 敵を全滅させてバトルが勝利した際、Zustand store で `isVictory` が即座に `true` に更新されるが、フロントエンドのメッセージ同期とタイプライター再生が完了する前に Victory 画面がオーバーレイ表示されてしまってはならない。
  - タイマーによるポーリング判定は非同期な状態更新（`validate-result` や `userProfile` の更新など）と競合して誤検知しやすいため、実際にタイプライター表示完了したログ配列数 (`displayedLogs.length`) と、ストアから届いた全メッセージから非表示の同期用メッセージを除外した実テキストログ数 (`battleState.messages.filter(m => !m.startsWith('__')).length`) を直接比較して、双方が一致するまで遷移をガードする実装規約を徹底すること。

## 4. ステータス・スキル・装備品モーダルの設計原則 (v28)
- **タブUIの廃止と2x2グリッド配置**: ステータスモーダルにおけるタブ切り替えUIは廃止され、生命力バーの下に2x2グリッド（スキルデッキ、装備品、所持品、パーティ）のボタンを配置し、それぞれ独立したサブモーダルをオーバーレイ表示（createPortal）する。
- **アコーディオン（折りたたみ）とフィルタ**:
  - 装備中のスキルおよび装備中のスロット（武器、防具、アクセサリー）は折りたたまず、常時展開して表示すること。
  - ストック一覧（所持スキル、NPC支援カード、武器、防具、アクセサリー）はカテゴリごとに折りたたみ（開閉）可能とすること。
  - 所持スキルストックには、コスト（All, C0, C1, C2, C3+）によるフィルタコントロールを提供すること。
- **クエスト進行中の変更制限**: クエスト進行中は、スキルや装備品、およびパーティメンバーの並び替え（ソート順）の変更を禁止する制御（`questLocked` などによるロック）を維持・徹底すること。
- **パーティメンバーの並び替え機能 (v28.1)**:
  - パーティモーダルでは、同行NPC of 並び順を変更する「▲」「▼」ボタンによるソート機能を提供し、データベースの `party_members.sort_order` と同期させること。
  - バトル時のNPC支援カードの注入優先順や、身代わり防御順（左＝先頭メンバー優先）を意図通りコントロールできるよう正確な順序管理を行うこと。

## 5. 進行不能不具合に対する補填およびデータ抽出手順
- **被災ロケーションの特定**:
  - クエストやコロシアムの遷移ロック等の不具合により、ユーザーがリタイアせざるを得なかった場合、その被災時のロケーション名（`location_name`）やペナルティ値は、`user_chronicles` テーブルの `event_type = 'quest_failure'` またはそれに類する失敗・放棄タイトルに紐付くレコードから特定すること。
  - `user_chronicles` からの抽出クエリは、Supabaseのデフォルトの 1000 件取得制限を回避するため、必ずユーザーID単位で個別にクエリをループ実行し、漏れなくデータを取得すること。
- **補填データの安全な適用と自動アサーション**:
  - 補填実行スクリプト（`apply_compensation.js` 等）では、適用前の値（事前値）と補填値、および適用直後に再クエリした事後値を比較し、「事前値 + 補填値 === 事後値」がゴールド、名声スコア、インベントリ数量等の全項目で成り立つことをプログラム的に自動アサーション検証する規約を徹底すること。

## 6. クエスト内一時アイテムの所持判定 (check_delivery / check_possession) に関する実装規約
- **判定対象の拡張**:
  - クエスト進行中に `reward` ノード等で獲得した一時アイテムは、クエスト完了（`/api/quest/complete`）までデータベース（`inventory` テーブル）に永続化されず、フロントエンドの戦利品プール（`questState.lootPool`）に一時蓄積される。
  - そのため、クエストの道中で `check_delivery`（納品判定）や `check_possession`（所持判定）を行うノードでは、必ず **「データベースのインベントリ所持数」と「戦利品プール内の同名アイテム数」の両方を合算して判定する** こと。これを怠ると、一時アイテムの所持判定が必ず失敗する不具合が発生する。

## 7. 同行NPCの耐久度・HP・VITの表示および摩耗に関する設計原則 (v28.3)
- **耐久度(durability)とバトルHP、VITの区別とマッピング**:
  - `party_members` テーブル of `durability` はNPC of 寿命であるVIT（Vitality）であり、最大値はすべてのNPCで一律 **`100`** 固定（初期値100）である。
  - クエスト完了ごとに、成否や戦闘不能 of 有無に関わらず、各パーティメンバー個別に **`1〜5` のランダム値** が減算され、0以下になったNPCは離脱（物理/論理削除）する。
  - `max_durability` は雇用時の最大HPスナップショットであり、戦闘時のHP上限である。HP of 現在値データはデータベースに保存せず、非戦闘時（街やステータス画面）は常に最大HPで全快表示される。
  - クエスト進行中の連続戦闘時のみ、戦闘終了時の残りHPがクライアント側のZustandストア（`questState.partyHp`）に一時退避され、連戦時の初期HPとしてマージされる。また、クエスト中の非戦闘UI（移動画面やステータス画面）でもこのZustandの値がマージされて現在HPとして表示される。
  - 宿屋の宿泊（`/api/inn/rest`）の際は、戦闘不能メンバーの生存フラグ（`is_active = true`）を復帰させるのみとし、寿命（`durability`）は全回復させない。
  - クエスト結果画面での寿命減少表示は、「耐久 100 ▸ 97」のように「耐久」ラベルを使用して摩耗を通知する。
- **離脱時の形見（メメント）の生成とフォールバック**:
  - 寿命（durability）が0以下になったNPCが離脱する際、その第1固有スキル（`inject_cards[0]`）に対応する `items` テーブルのアイテム（`linked_card_id` で紐づくもの）を形見アイテムとしてインベントリに付与する。
  - 対応するアイテムが存在しない基本スキル（石投げ、防御、強打など）の場合、フォールバックとして「物資ボックス (ID: 701, slug: 'item_supply_box')」を付与する。

## 8. バトル勝利時の非同期処理によるUI制御（ブロッキング回避） (v29.0)
- **非同期・非ブロッキング処理の原則**:
  - バトル終了時の勝利処理（`/api/report-action`, `/api/battle/validate-result`, `/api/profile/update-status` の呼び出しや、`fetchWorldState`, `fetchUserProfile` による再取得など）は、UI表示（メッセージや勝利画面）を妨げてはならない。
  - バトル進行アクション（`runNpcPhase` 等）の中でこれらの非同期通信を `await` してしまうと、ネットワーク環境やDB書き込みの遅延がそのままテキストログ表示のタイムラグや画面硬直となって现れる。
  - そのため、勝利確定時は即座に `isVictory: true` やログメッセージをステートに `set` して画面描画を更新し、上記のデータベース更新やサーバー検証APIは `await` せずにバックグラウンド（Promise または `.then()` チェーン）で並行実行する設計を徹底すること。

## 9. 閲覧施設（コレクション・クエスト記録・ランキング）の配置設計原則 (v30.0)
- **共通の表示と閲覧**:
  - 「コレクション（図鑑）」「クエスト記録」「ランキング」の3つの施設は、旅人の拠所（ハブ）だけでなく、通常拠点（宿屋/酒場）のメニューにも一貫して表示させる。
  - これらは閲覧専用 of 施設であり、いずれの拠点から開いた場合でも動作やデータ取得のロジックに差分を設けてはならない。共通の API およびモーダルコンポーネントを流用して表示すること。
- **世界情勢変化ポップアップの非表示・廃止**:
  - 6時間ごとの世界情勢変化時に拠点アクセスした際、自動で表示されていた警告ポップアップ（`WorldChangedModal`）および「号外（`ChronicleModal`）」の自動ポップアップ表示は廃止されたため、今後も復活させてはならない。過去の情勢変化の履歴は、歴史館（`HistoryArchiveModal`）からのみ閲覧可能とする。

## 10. 気の癒やし・デトネーション・HP装備補正維持に関する仕様規約 (v35)
- **気の癒やし（回復+リジェネの同時適用）**:
  - `case 'heal'` アクション内で `effectInfo.effectId`（リジェネ等）が存在する場合、回復と同時にバフの付与も行うこと（`skipDamage: true` 時の共通バフ付与スキップ仕様と干渉させない）。
  - NPCのターン処理（`processPartyTurn`）では、アクションが `buff` であっても、紐づくカードが回復タイプの場合は回復処理（`healAmount`）も並行して適用すること。
  - NPCが自分以外の特定メンバーに単体バフ・回復をかける際、そのバフが caster (発動者自身) ではなく、正しく `action.targetName` の味方NPCに適用されるようターゲット名と順序を特定して適用すること。
- **デトネーションのミス時手札破棄**:
  - 攻撃ミス判定（`rollMiss`）が成功して早期リターン（`return true`）する場合であっても、カードの `effectType` が `detonation` の場合は手札からのランダム2枚除外（Exhaust）を漏れなく発動させ、除外されたログをメッセージに追加すること。
- **クエスト進行を通じたHP装備補正値の維持**:
  - クエスト開始パラメータ（`startParams`）の `playerMaxHp` は、ベース最大HPではなく、必ず装備HP補正値（`equipBonus.hp`）を加算した実効最大HPとして初期化すること。
  - クエスト完了時（`/api/quest/complete`）のレベルアップ更新（`updates.hp`）およびクエスト失敗ペナルティによるHP復旧時、およびクエスト内の `healParty` による回復上限キャップ時に、装備HP補正分を切り捨てずに、新最大HP＋装備補正値の範囲で現在HP（`hp`）を復帰・維持すること。
- **スキルデッキの装備・装備解除（外す）における連打・重複処理の防止**:
  - スキルデッキ管理（`SkillDeckModal`）において、スキルの装備・装備解除（外す）アクションを実行する際は、非同期リクエストの実行中（`togglingId !== null`）すべてのアクションボタンを `disabled` にすること。
  - 重複リクエストや連打による多重送信を防ぐため、実行処理の先頭で `togglingId` の有無による早期リターン等の排他制御（連打防止）を行うこと。

## 11. クエスト中チェックポイント、他プレイヤー遭遇、および前景画像（fg_image）の仕様規約 (v36)
- **チェックポイント確定セーブの挙動と連動**:
  - クエスト中の野営地（`camp` ノード）の処理開始時に、API `/api/quest/checkpoint` を呼び出し、クライアント側の `lootPool`, `consumedItems`, `reputationChanges` をデータベースに永続化させること。
  - セーブ成功時は速やかにクライアントの `lootPool` と `consumedItems` をクリアし、`fetchUserProfile()` と `fetchInventory()` を並行して呼び出してローカル状態を最新化すること。これにより、野営モーダルを開いた直後から獲得したアイテムを装備可能にする。
- **同一拠点プレイヤーとの遭遇イベント**:
  - `POST /api/quest/meet-player` エンドポイントでは、プレイヤーの現在地と同じ拠点にいる自分以外のユーザー名をランダムに1人取得する。
  - 他のユーザーが存在しない、あるいはエラーが発生した場合は、必ずNPCマスタデータやダミーの冒険者名（「冒険者ジーク」など）をフォールバックとして返却し、空の文字列やエラーを返さないこと。
  - 取得したプレイヤー名はクライアント側の `questState` のフラグ（`met_player_name`）に保持し、テキスト描画時に文字列内の `{met_player_name}` プレースホルダーをすべて動的に置換する処理を徹底すること。
- **前景画像レイヤーの描画 (fg_image)**:
  - ノードデータ（`fg_image` または `params.fg_image`）が指定されている場合、`ScenarioEngine.tsx` は背景画像レイヤーとダイアログボックスの間のレイヤーに画像を重ねて描画すること。
  - 前景画像はダイアログボックスに表示される `speaker_image_url`（顔アイコン）とは異なり、画面中央付近に立ち絵や宝箱として表示されるため、最大高さ（例: `max-h-[55%]`）や位置調整マージンを設定し、不快なアセット切り替えを防ぐためのフェードインアニメーションを適用すること。

- **チェックポイント・他プレイヤー遭遇仕様の補足 (v36.1)**:
  - **Zustandストア状態更新の整合性**: `reputationChanges` などの一時状態をクリアする際は、ストアの状態を直接ミューテートする（例: `useQuestState.getState().reputationChanges = ...`）のではなく、Zustandの反応性（Reactivity）を保証するため、必ず `useQuestState.setState({ reputationChanges: {} })` のように `setState` 経由で更新すること。
  - **questFlags の文字列値保存と上書き制御**: `questFlags` は `Record<string, number | string>` 型に拡張されており、プレイヤー名などの文字列も保持できる。文字列をセットする際は、加算演算子によるバグ（例: `0` に `"プレイヤー名"` が加算されて `"0プレイヤー名"` と文字列結合される問題）を防ぐため、必ず `setFlag('met_player_name', player_name, true)` と第3引数 `isSet: true` を明示的に指定して上書き代入すること。また、テキスト表示側の置換処理 `{met_player_name}` では、型エラーを防ぐため必ず `String(questState.getFlag('met_player_name'))` のように文字列キャストを適用すること。
  - **サーバーサイド API 引数の整合性**: チェックポイントAPI `/api/quest/checkpoint` にて `reputation_changes` を適用する際は、`grantReputationChanges` に `supabase, user_id, reputation_changes, user.current_location_id` の 4 つの引数を正しく引き渡して、アライメント更新および location 名声更新を一括適用すること。

## 12. 魔術学院機能拡張（黎明の知識と古の契約・鍵アイテム・スライドUI）に関する仕様規約 (v37)
- **既存スキルへのレアリティ定義**:
  - ベーシックパック収録カードだけでなく、すべての既存スキル（ID 3011〜3088, 3141〜3142）に対して、共通定義ファイル `src/lib/rarity.ts` 内で SR/R/U/C のレアリティマッピングを定義する。DBのスキーマ変更は行わない。
- **鍵アイテムのショップ除外と名称・説明の定義**:
  - パック開封用の鍵アイテムである `item_basic_key` (ID 76) および `item_academy_key` (ID 77) は、通常ショップに並ばないよう `items.csv` 上の `nation_tags` を空とし、さらにショップAPI `SHOP_EXCLUDE_SLUGS` に登録して除外を徹底する。
  - `item_basic_key` の名称は「知識と契約の鍵」、説明文は「カードパック「黎明の知識と古の契約」を開封することのできる鍵」とする。
  - `item_academy_key` の名称は「魔道と知識の鍵」、説明文は「カードパック「混沌の魔道と反逆の鉄壁」を開封することのできる鍵」とする。
- **ベーシックパック「黎明の知識と古の契約」の基本仕様**:
  - 販売価格は `3,000 G`、重複カード出現時のキャッシュバック返還額は `300 G` とする。
  - ベーシックパックのフリップ演出におけるカード裏面画像は、専用の青銅魔法円デザイン（`/images/card_back_basic.png`）を適用して他シリーズと分離する。
- **魔術学院UIの切り替えおよびカードリスト仕様**:
  - パックシリーズの切り替えは、今後の拡張性を見据えて従来のタブ切り替えではなく、左右の矢印ボタン（またはスワイプ）によるカルーセル（横スライド）方式とする。
  - 各パックのグラフィックの右下に「カードリスト確認」のアイコンボタンを配置し、クリックで収録カードがレアリティ（SR/R/U/C）ごとに整理されたグリッドギャラリーをモーダル上に表示させること。

## 13. マネタイズおよび課金統合モーダルに関する開発ルール (v38)
- **課金UIの統合とカプセル化**:
  - 課金、プラン加入、都度購入に関するすべてのUIは `BillingModal.tsx` に集約・カプセル化すること。設定画面などの他のモーダルに直接課金入力や購入ボタンを分散配置してはならない。
  - 特商法に基づく表示への同意チェックボックス（利用規約、返金不可などの確認）を必ず決済開始前にチェック必須にすること。
- **アカウント限定パッケージの二重購入防止**:
  - スターターパック・エリートパック等の「アカウント1回限り購入可能」な商品は、以下の3レイヤーすべてで厳密にガードをかけること：
    1. **フロントエンド**: `userProfile.has_purchased_starter` / `has_purchased_elite` フラグを参照し、購入済みの場合はボタンを非活性化（「購入済み」と表示）。
    2. **Checkout API (`/api/billing/checkout`)**: JWT認証でユーザーを特定後、DBから対応する購入フラグを直接取得してチェックし、すでに `true` の場合は `400 Bad Request` を返す。
    3. **Stripe Webhook (`/api/webhooks/stripe`)**: 決済完了処理の中で、PostgreSQL RPC (`process_package_purchase`) を呼び出す。このRPC内部で、購入フラグがすでに `true` の場合はトランザクションを即座にロールバック（または早期リターン）するアトミックなガードを行う。
- **ゴールドと鍵のアトミックな付与処理**:
  - サブスクリプションのWeeklyボーナス（ゴールド＋鍵）や限定パッケージ購入時のアイテム付与は、DBレベルのトランザクションを保証するため、フロント側やAPI側で個別に複数回クエリを実行してはならない。必ず PostgreSQL RPC (`process_weekly_subscription_bonus` / `process_package_purchase`) を介してアトミックに実行すること。
  - 鍵アイテム（`item_basic_key` (ID: 76) や `item_academy_key` (ID: 77)）をインベントリに付与する際は、必ず `inventory` テーブルに `quantity` 加算を行う upsert ロジック（`ON CONFLICT (user_id, item_id) DO UPDATE SET quantity = inventory.quantity + EXCLUDED.quantity`）を使用し、レコードの重複や上書き消失を防ぐこと。
- **魔術学院等のモーダル連携**:
  - 魔術学院 (`AcademyModal.tsx`) など、他のモーダルや画面から課金画面を起動する場合は、宿屋の共通ページステート (`useInnPageState.ts` や `page.tsx`) を経由して `BillingModal` を上に重ねてレンダリングする（オーバーレイ表示）設計とすること。モーダルを閉じたあとに元のモーダル（魔術学院等）に戻れるよう、モーダルのZ-indexやオーバーレイ順序を適切に制御すること。
- **既存加入者のアップデート時一括補填**:
  - 特典アップデート等により、既存のサブスクリプション加入者に対してアイテムの一括補填が必要な場合は、`supabase/migrations/` 配下にデータ移行用の移行SQLファイルを作成し、デプロイ時に一度だけ安全に実行する設計とすること。

## 14. オンボーディング・本登録連携に関する不具合対策とリダイレクト制御 (v38.1)
- **クエスト進行ノードの復旧時ガード**:
  - クエストページ（`page.tsx`）の初期マウント時に Zustand ストアから前回の進行中ノード（`currentNodeId`）を復元する際は、必ず現在開いているクエストIDと保存されている `questId` が一致していることを検証すること。
  - IDが異なる場合、古いノードIDを初期ノードとして使用してはならない。初期状態（`start`）からクエストを開始させ、存在しないノードへの遷移によるデータ異常エラー（リカバリー画面）の発生を防止すること。
- **OAuthコールバック処理におけるコードパラメータのクリーンアップ遅延**:
  - OAuth連携（Google等）後に `?code=xxx` などのパラメータを伴ってリダイレクトバックされた際、URL からパラメータを即時削除してはならない。
  - パラメータの削除は、Supabaseクライアントによるトークン交換およびセッションの書き換え（匿名アカウントの移行）が完了したことを `onAuthStateChange` または `getUser()` 等で確認した上で実行すること。早期消去によるセッション更新の失敗を防ぐこと。
- **モバイルナビゲーションバナーの見切れ防止**:
  - 拠点上部等の案内ナビゲーションにおいて、文字数が変化する可能性があるテキスト表示領域には、モバイル端末での見切れ（`...` での切り捨て）を防ぐため、安易に `truncate` クラスを使用してはならない。改行を許可（`whitespace-normal` や `break-all` 等）し、全文が確実に読めるようにレスポンシブ配置を設計すること。
- **OAuthセッション更新時のメモリトークンキャッシュ破棄**:
  - `authToken.ts` などでJWTアクセストークンをメモリ上にキャッシュする仕組みがある場合、Google連携本登録（`linkIdentity`）やログインでSupabaseのセッション状態が切り替わった直後、必ず `clearAuthTokenCache()` などのキャッシュクリア関数を明示的に実行し、古いトークンがAPIリクエストのヘッダーに混入するのを防ぐこと。これを怠ると、サーバー側で古いセッションとして判定され、同期処理が漏れる原因となる。
- **拠点モーダルのSWRキャッシュ化とプリフェッチ**:
  - 宿屋での酒場、道具屋、クエストボード等の挙動の軽量化（ローディング排除）のため、各データはZustandストアにキャッシュスロットを設けてSWRパターン（キャッシュ優先ロード）を適用すること。
  - オンボーディング第1話等のプレイ中に、バックグラウンドの非同期処理で拠点一括データ（`/api/init-page?prefetch_quest_id=6001`）および道具屋データ（`/api/shop`）をプリフェッチしてZustandに格納しておき、チュートリアル完了後に拠点に戻ってきた際、すべてのモーダルがロードなしで一瞬で起動する快適なUI体験を実現すること。
  - クエスト完了時には通常キャッシュがクリアされるが、第1話（6001）完了時のみは例外的にクエストボード等のキャッシュクリアおよび `lastInitPageFetchTime` の初期化を抑止し、プリフェッチされたクリア状態のデータをそのまま用いて超高速（ロード時間ゼロ）で第2話を表示すること。

## 15. 描画・遷移最適化およびGoogle連携の強制アカウント選択に関する実装規約 (v39.0)
- **Next.jsのルートプリフェッチによる画面遷移ラグ解消**:
  - `router.push('/inn')` や `router.push('/workshop')` などの非同期遷移を行う画面（クエスト詳細等）では、マウント時に必ず `router.prefetch` を用いて遷移先ルートのコードチャンクを先行取得すること。
- **Google OAuth 連携（linkIdentity）におけるアカウント選択・同意の強制**:
  - すでに他のアカウントでGoogleログイン中のブラウザやモバイルWebviewにおいて、勝手に自動連携・即時リダイレクトバックされてしまうのを防ぐため、`supabase.auth.linkIdentity` の `options` に `queryParams: { prompt: 'select_account consent' }` を指定して強制的に Google のアカウント選択画面および同意承認プロンプトを表示させること。
- **拠点施設NPC画像の先行バックグラウンドロード**:
  - 宿屋（拠点）ページのマウント後、表示対象となる5施設（宿屋、ギルド、商店、神殿、魔術学院）のNPC画像を `getNpcForLocation` から早期に解決し、`new Image().src = imageUrl` で非同期にプリロードしておくことで、各施設をタップして `NpcDialogModal` を開いた際のアセット読み込み遅延（ポップイン）を排除すること。
- **外部連携ローディングUIの視覚的フィードバック**:
  - 設定モーダル等から Google アカウント連携（`linkIdentity`）を呼び出す際は、単にボタンを non-clickable にするだけでなく、ボタン内にローディング表示（スピナーおよび「Google 認証を開始中...」等の状態通知）を即座に表示してフリーズ感を解消すること。

## 16. インタラクションボタンの連打防止・応答性向上に関する実装規約 (v40.0)
- **閉じる処理における非同期ローディング画面の原則禁止**:
  - 各種モーダル（酒場、クエストボード、ショップ等）を閉じる処理において、不要な `isClosing` フラグによるフルスクリーンローディング画面の割り込みや `setTimeout` による遅延を発生させてはならない。タップ後、即座に同期処理として `onClose()` を呼び出して画面をアンマウントすること。
  - 閉じるボタンを連打されるのを防ぐため、クリックされた瞬間にボタン単体を即座に `disabled` 化しつつ同期的に閉じる。
- **進行・決定系ボタンの多重入力ガード（連打防止）**:
  - クエスト進行中の「次へ」ボタンや会話選択肢 (`ScenarioEngine.tsx`) のように、画面遷移を伴うインタラクションボタンには、タップ後数秒〜数百ミリ秒間（200〜300ms推奨）の入力インターバル（遷移ロック）を設け、連打による複数ノードのスキップを確実に防止すること。
  - 施設NPCダイアログ (`NpcDialogModal.tsx`) などにおけるメインおよびセカンダリの処理開始ボタンは、クリックされた瞬間にすべてのボタンを即座に `disabled` 化し、テキストを「読み込み中…」等に切り替えて明確なローディングフィードバックを提供すること。
  - 遷移ロック状態が解除されずに進行不能（ソフトロック）になるのを防ぐため、戦闘開始ボタンなど画面切り替えが発生するボタンについても確実に一定時間後にロックを解除するタイマー（setTimeout等）を設定するか、外部からのノード復元（`initialNodeId` の変更検知時など）の際にステートを明示的に初期化すること。
## 17. 決定/遷移時における応答性（即時ローディング）とオンボーディングガイド順序の是正規約 (v41.0)
- **非同期の遷移・決定処理における即時フィードバック**:
  - クエスト結果画面（`QuestResultModal`）のように、画面遷移や大きな処理を伴う「冒険を続ける」ボタン押下時は、ボタンの無効化だけでなく、モーダル最前面にフルスクリーンローディング（「拠点に移動中…」または「クリエイターズ工房に戻る中…」）を即座に（同期的に）描画し、非同期のページロード中もユーザーに明確な進行状況を伝えること。
  - 酒場（`TavernModal`）の初期マウント時のように、API等から非同期にデータをロードする際は、プレースホルダーテキストの単なるパルス点滅だけでなく、回転スピナー（`Loader2` 等）を画面中央に明示的に描画し、フリーズ感を排除すること。
  - 酒場での仲間雇用（「契約を結ぶ」）などの非同期通信を伴う決定処理において、処理中のボタン内に回転スピナーと「手続き中…」などのテキストを即時描画してボタンを非活性化すること。`setTimeout` などで非同期 API 呼び出しを別スレッドに逃がして同期処理（モーダルクローズ等）を先行させてはならない。必ず Promise チェーンを `await` し、ローディング中（`hirePhase === 'loading'`）を視覚的に明示した上で、通信完了後に次の画面（加入完了オーバーレイ等）へステップバイステップで遷移すること。
- **オンボーディングガイドにおける「出発する」の最後尾化**:
  - チュートリアル第1話クリア後の拠点ガイドにおいて、魔術学院・道具屋/チャージ・ステータス・設定などの施設解説をスキップしてワールドマップ（「出発しよう」）へ遷移してしまわないよう、必ず他のすべての施設・画面を訪問した後に「ワールドマップから出発」するようガイド優先度を設定すること。
  - 優先順位: 仲間雇用 ➔ ギルド ➔ 魔術学院 ➔ 道具屋・チャージ ➔ ステータス ➔ 設定 ➔ ワールドマップ（出発）

## 18. クエストクリア後のアカウント認証プロモ判定時 of 誤タップ防止およびツアー開始遅延制御に関する技術的制約 (v4.9)
- **プロモーション表示判定中 of インタラクションブロック**:
  - クエスト帰還直後（`questJustCleared`）などでプロモモーダル（ゲスト登録プロモ、スターターパックプロモ） of 表示判定やロードが行われている間、背後 of 施設ボタンやヘッダーがタップできてしまうことを防ぐため、必ず `isPromoPending` のような判定中フラグを導入し、画面最前面に透明なガードカバー（`fixed inset-0 z-[300] bg-transparent pointer-events-auto`）を表示してすべての入力をブロックすること。
- **オンボーディングツアー開始 of 遅延（プロモ閉じるまでの保留）**:
  - プロモーション of 表示判定中または表示中である間は、ツアー of 自動開始（`onboardingTourStep = '1'`）を抑制すること。
  - プロモモーダルが閉じられた際、または表示不要と確定した段階で初めてツアーを開始（ステップ `'1'` に設定）させ、プロモとツアー of 不整合を防ぐこと。

## 19. プロモーション待機ガードの完全持続と遷移前 await によるフリーズ防止に関する技術的制約 (v4.9.1)
- **プロモ表示中における操作ガードカバーの持続と z-index 設計**:
  - プロモモーダルが表示されている（または表示準備中）の間は、誤クリックを防ぐガードカバー（`z-[300]`）をアクティブに保ち、プロモモーダルが閉じられたハンドラ内で初めてガードを解除（`isPromoPending = false`）すること。
  - この間、プロモモーダル（`GuestRegisterPromoModal`, `StarterPackPromoModal`）自体の z-index を必ず **`z-[1000]`** に引き上げ、ガードカバー（`z-[300]`）より手前に表示させることで、モーダル内部のボタン操作を妨げずに背後の誤操作を防ぐ。
- **遷移前非同期処理の await の徹底**:
  - `QuestResultModal` の `onClose` のように、画面遷移（`router.push`）を行う直前で行うプロフィール等の再取得非同期処理（`fetchUserProfile()` 等）は, 必ず **`await` を付与して完了を待つ** こと。
  - `await` を怠ると、Next.js のルーター遷移処理が即座にメインスレッドをブロックし、ボタンの「読み込み中…」などの React ステート更新がブラウザにペイントされる前に画面がフリーズしてしまい、ボタンが変わらない不具合を引き起こす。
- **リザルトローディングオーバーレイの前面化**:
  - クエスト結果画面等における遷移中のローディングオーバーレイ（`isClosing && (...)`）は、z-index を **`z-[2000]`** に引き上げ、カードや背面にあるシナリオエンジンの古いスピナー（「画面を切り替え中...」）などを完全に覆い隠し、不細工な透過残像をシャットアウトすること。

## 20. ツアー自動開始のレースコンディション防止およびState駆動の描画保証付き遷移に関する技術的制約 (v4.9.2)
- **ツアー自動開始の二重ガードとハイドレーション修正**:
  - 宿屋遷移時のツアー自動開始判定において、プロモ判定の遅延や isPromoPending の初期化ラグによるツアー誤開始（ステップ '1' への初期化）を防ぐため、ツアー開始判定内で sessionStorage.getItem('wirth_dawn_quest_just_cleared') === 'true' も直接チェックし、プロモーション保留期間中の自動開始を完全にブロックすること。
  - isPromoPending などの sessionStorage 依存のステートは、ハイドレーションミスマッチを防止するため初期値を false とし、マウント時の useEffect で安全にクライアント側の値と同期させること。
- **State駆動の描画保証付き遷移 (Deferred Transition)**:
  - 遷移（router.push）が発生するモーダルのクローズ処理において、ボタンの「読み込み中…」や全体オーバーレイの描画（Paint）を遷移前に確実に完了させるため、クローズイベントハンドラで直接 onClose を呼ばず、isClosing === true を検知する useEffect 内で setTimeout (150ms 程度) を挟んでから onClose を実行する設計を徹底すること。これにより、ブラウザの描画を確実に画面にピクセル反映させてから遷移処理にスレッドを明け渡す。

## 21. スクロール位置残存による座標ズレ（ヒットテストバグ）防止およびモーダル背景クリック突き抜け対策 (v4.9.3)
- **遷移直後のスクロール位置リセットと body のスクロール禁止**:
  - クエスト画面等の長いスクロールが発生するページから、画面全体を覆うレイアウト（宿屋等）に遷移する際、body にスクロール位置が残っていると、ブラウザは描画位置とDOMの論理クリック当たり判定にズレを生じさせる（ヒットテストバグ）。
  - これを防ぐため、マウント時に window.scrollTo(0, 0) および body, documentElement の scrollTop = 0 を強制実行し、且つその画面の表示中は document.body.style.overflow = 'hidden' を設定して不要なスクロールを完全に抑制すること。
- **同期プロモガード（インタラクションの全無効化）**:
  - useEffect の非同期ステート反映待ちによる誤タップを防ぐため、クライアントサイドでのマウント直後の最初のレンダーから sessionStorage 内の帰還フラグを同期チェックすること。
  - プロモ保留判定期間中（isPromoGuarded）は、操作ガードカバーを即時描画するだけでなく、施設ボタン・ヘッダーボタン・マップ遷移等の全コールバックハンドラを null / undefined にするか早期 return させ、インタラクションを二重に無効化すること。
- **モーダル背景の pointer-events ガード**:
  - プロモーション等のモーダル背景マスク要素（透過黒背景）には pointer-events-none を付与せず、必ず **pointer-events-auto** とし、背後の画面要素（施設ボタン等）にクリックイベントが透過するのを物理的に遮断すること。

## 22. Next.js 非同期スクロール復元と overflow: hidden の競合（スクロールロック）対策 (v4.9.4)
- **遅延多段階強制スクロールリセットの義務化**:
  - Next.js 遷移時の非同期スクロール復元（Scroll Restoration）はマウント「後」に走るため、マウント時 `useEffect` の即時リセットが後から上書きされる。
  - この結果、ズレた位置で `overflow: hidden` がかかりタップ座標が恒久的にズレる（スクロールロックバグ）。
  - これを解消するため、マウント時の `useEffect` で即時リセットを行うと同時に、`setTimeout`（`0ms`, `50ms`, `150ms`, `300ms`）を用いて段階的にスクロール位置を `0` にリセットし続けることで、非同期スクロール復元を完全に上書きして `0px` に強制着地させ、タップ領域ズレを完全に根絶すること。

## 23. 街の噂話（BBS）および自己紹介ポップアップの設計・実装原則 (v17.1)
- **タイムライン型BBSモーダル (`GossipModal.tsx`)**:
  - **形式と配置**: モーダル形式を維持し、右下に「＋」のフローティングアクションボタン (FAB) を設置。タップで140文字制限の投稿ポップアップ（投稿／キャンセル）を立ち上げる。
  - **タイプライター演出の完全撤廃**: 読み込み速度と操作性を損なわないよう、最上段固定メッセージを含め、すべてのテキストのアニメーション表示および点滅カーソル表示を禁止し、即時描画（または標準フェードイン）とすること。
  - **10秒のクールダウンチェック**: 連打ハラスメント防止のため、直前投稿時刻から10秒未満の連続投稿をAPI側およびフロントエンドのFAB押下時で拒否すること。その際の警告メッセージは必ず「連続投稿は禁止となります」とすること。
  - **上下フリックによる再読み込み（プル・トゥ・リフレッシュ）**: スクロールが最上部にある状態で下にフリックして指を離すと、タイムラインを最新情報に更新する。
  - **DB負荷防止の3秒スロットリング**: 短時間での引き下げリロード連打によるDB負荷を防ぐため、3秒以内の連続再読み込み要求は、APIリクエストをスキップしてローディング演出のみを模擬すること。
  - **禁止ワードフィルタ**: 指定のハラスメント/不適切ワードを含む投稿をAPI側でブロックすること (`400 Bad Request`)。
  - **システム自動投稿**: アカウント名 `世界の観測者` および宝石モチーフの専用アバター (`/images/icons/observer_gem.png`) を用い、最新1件をタイムライン最上段に特別な背景色でピン留め固定すること。
  - **トリガー定義**:
    - クエストクリア: メインクエストEp15 (6015), Ep20 (6020), スポット高難度 (6101-6104), 伝説級高難易度 (6105-6111) の成功時。
    - 称号獲得: Tier S (光輝の守護聖者, 終末の覇王, 天秤の調停者, 嵐の解放者, 不滅の古豪, 神話の富豪) および Tier A (聖騎士, 暗黒卿, 義賊, 冷徹な執行者, 黄金の暴君, 清廉の騎士団長, 戦乙女, 魔女) の獲得時。
    - **追加の偉業・ランキング獲得トリガー (v17.5)**: 図図鑑完成 (`collection_complete`), 拠点名声の英雄到達 (`fame_hero` / $\ge 200$), 犯罪王到達 (`fame_villain` / $\le -200$), 名声ランキング1位獲得 (`ranking_fame_1st`), アライメントランキング1位獲得 (`ranking_alignment_1st`)。それぞれ重複防止（1回限り）を適用して投稿すること。
    - **管理者アカウント除外ルール**: 本番のきたむ様アカウント（`c1cf67dd-527a-497e-bf88-ce10c2cb516f`）およびテストアカウントによるイベントは、システム自動投稿 of トリガー対象から必ず除外すること。
    - **投稿順序制御**: 6時間バッチ時の複数同時投稿の際、タイムライン上で世界情勢変動（繁栄度変化）が最新（最上段）に並ぶよう、ランキング系の投稿時は `created_at` を現在時刻より数秒（5秒程度）巻き戻してインサートすること。
- **自己紹介ポップアップ (`SimpleUserProfilePopup.tsx`) & タイムラインアバター装飾**:
  - 表示項目を「称号」「ユーザー名」「ユーザーアイコン」「レベル」「ゲーム内年齢」「自己紹介文」「購読プラン」の7点に拡張し、調和したステータス表示としてレイアウトすること。
  - アバター枠にユーザーの購読プラン（`subscription_tier`）に応じた専用グラデーションフレームを適用すること（Premium: 金グラデーション, Basic: 青グラデーション, Free: 通常）。
  - 表示データは、プレイヤーをタップした際に `/api/profile?profileId=[ID]` から非同期取得した最新のプロファイル情報（`subscription_tier` を含む）を同期させる設計を維持すること。
- **未閲覧ユーザー向け最新投稿バルーン表示**:
  - `localStorage` に保存された最終閲覧日時（`last_viewed_gossip_time`）と現在の最新投稿日時を比較し、未読の最新投稿がある場合のみ、噂話アイコンのすぐ下に翡翠枠の上品なフキダシ型ダイアログ（`border-2 border-emerald-500 bg-slate-950`）を表示し、最新投稿（3行省略）を表示してアピールすること。
  - ユーザーが噂話モーダルを開く、またはバルーンの「✕（閉じる）」ボタンをクリックした際に、`localStorage` の最終閲覧日時を最新投稿日時に更新して既読扱いとすること。
  - オンボーディングチュートリアル進行中（`isTourActive === true`）は、ガイダンスバナーとのバッティングを防ぐためバルーンを非表示とすること。
- **システムメッセージ非表示（フィルター）機能**:
  - 噂話（BBS）画面に「システムメッセージ非表示」トグルスイッチまたはボタンを配置すること。
  - トグルの選択状態は `localStorage` の `wirth_dawn_gossip_hide_system` キーに保存し、再読み込みやリロード後も状態を永続化すること。
  - フィルター有効時は、最上部ピン留め投稿（`pinned_system_post`）およびタイムライン内のシステム投稿（`is_system === true`）を非表示とすること。
- **旧仕様のクリーンアップ**:
  - `rumors` テーブルおよび `scripts/sync_rumors.ts` を廃止・削除すること。
  - `src/app/api/gossip/route.ts` および `src/app/api/init-page/route.ts` から旧4タブ用のクエリ（`world_states_history`, `rumors`, `scenarios` の特別クエスト、および `ShadowService` の傭兵NPC）を完全に削除し、新しい `gossip_posts` の軽量なクエリへと移行すること。
  - `GossipModal.tsx` 内の旧タイプライターコンポーネント（`TypewriterCard`, `TypewriterCardWithNext`, `SequentialCards`）、タブ用定数、および酒場誘導ボタンを完全に排除すること。

## 24. 新オンボーディングガイドツアー再設計および第2話限定案内バナー (v5.0)
- **ツアー構成と順番**:
  - オンボーディングガイドツアーは全6ステップとし、進行順序は「宿屋/酒場 (Step 1) ➔ 道具屋 (Step 2) ➔ 魔術学院 (Step 3) ➔ ステータス (Step 4) ➔ ワールドマップ (Step 5) ➔ ギルド (Step 6)」とする。
- **モーダル表示制御**:
  - Step 1からStep 4、およびStep 5（ワールドマップ「出発する」クリック時）は、実際の施設モーダルやマップ画面を起動・遷移させず、単にステップを前進させる（ダミーボタン挙動）こと。
  - 最後の Step 6（ギルド）をタップした際のみ、実際にギルドのNPC会話ダイアログを起動し、クエストボード（`QuestBoardModal`）を開くように制御すること。
- **ツアーの完了判定**:
  - クエストボード（`QuestBoardModal`）の `onSelect` コールバック等において、Step 6（ギルド）の受注時に `onboardingTourStep` を `'completed'` に更新し、すべての拠点施設訪問フラグを `true` に設定してツアーを完了させること。
- **ツアー完了後の第2話限定案内バナー**:
  - ツアー完了後にアクティブなクエストを受注していない場合、第2話未クリア（`6002`）のユーザーに対してのみ、現在地（「国境の町」であるかそれ以外か）に応じて適切な案内メッセージバナーを宿屋トップに表示すること。第3話以降の案内バナーは表示しない。

## 25. コロシアム難易度別連勝ランキングおよび報酬傾斜配分に関する開発・実装ルール (v42.0)
- **難易度別連勝の正確なマッピング**:
  - コロシアムの戦闘結果を検証・更新する処理（`/api/battle/validate-result` や `/api/quest/give-up` など）において、挑戦中のコロシアムクエストの難易度（`easy`, `normal`, `hard`）を必ず判定し、対応する難易度の連勝カラム（`current_streak_easy`, `max_streak_easy`, `current_streak_normal`, `max_streak_normal`, `current_streak_hard`, `max_streak_hard`）のみを更新すること。
  - 難易度と無関係なカラムを誤って更新したり、リセットしたりしないように実装規約を徹底すること。
  - また、累計勝利数（`wins`）は難易度を問わず、全難易度を合算した総合値として常に更新すること。
- **6時間ごとの報酬付与における重複入賞・報酬額計算および空席処理**:
  - `/api/cron/daily-update` 内でのランキング報酬付与時、決定された「オプションA（推奨：難易度に応じた傾斜配分）」に基づいて各部門（累計勝利数、Easy連勝、Normal連勝、Hard連勝）の上位3名を取得し、アトミックにゴールドを付与すること。
  - 同一ユーザーが複数の部門で上位入賞した場合、報酬は切り捨てずにすべて加算（合算）して付与すること。
  - システム通知（`notifications`）に送信されるメッセージには、どの部門で何位に入賞し、それぞれいくら付与されたのかの内訳と合計金額を明確に記述すること。
  - **空席時の例外処理**: 挑戦者が3人未満で、対象順位（例えば3位など）に該当するユーザーデータが存在しない場合は、その枠を「空席」として扱い、ゴールドの付与や通知の送信を確実にスキップ（無視）するロジックとすること。
- **歴史（年代記）への記録の維持**:
  - 報酬配布完了時は、累計勝利数ランキングでの入賞だけでなく、各難易度別の連勝数ランキングで入賞した場合においても、通常通り個人タイムライン（`user_chronicles`）にインサート（イベントタイプ `system_reward`）して記録を残すこと（空席の場合は除く）。複数の部門で入賞した場合は、獲得ゴールドを合算した形で適切に記録を残すこと。
- **15分ごとのキャッシュ自動再集計仕様**:
  - ユーザーが最新の自分および他者の戦績情報を速やかに認識できるようにする観点から、ランキングキャッシュテーブル (`ranking_colosseum_cache`) は、6時間ごとのリセットとは別に、15分に一度バックグラウンドで自動再集計され、最新の状態に更新される仕様を維持すること。
- **ランキング履歴退避時のスキーマ整合**:
  - 報酬配布完了後の履歴退避（`ranking_colosseum_cache` から `colosseum_ranking_history` へのコピー）およびリセット処理において、新設された難易度別の連勝（`max_streak_easy`, `max_streak_normal`, `max_streak_hard`）および順位（`rank_by_streak_easy`, `rank_by_streak_normal`, `rank_by_streak_hard`）の全列を漏れなくコピーし、データの欠損を防止すること。
- **ゲーム内説明テキストおよびプレイガイドの整合性保証**:
  - コロシアムの報酬体系やランキング仕様を変更する際は、ゲーム内説明モーダル（`ColosseumModal.tsx`）の報酬・ルール表記、ランキング画面（`ColosseumRankingModal.tsx`）のマイステータスや注意書き、およびプレイヤー向けのプレイガイド（`docs/play_guide.md`）の該当箇所を漏れなく同期して更新すること。ゲームの実行仕様（API/DB）とユーザー向け説明（UI/ドキュメント）の乖離による不整合を完全に防止すること。

